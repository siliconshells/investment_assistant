apiVersion: v1
kind: Service
metadata:
  name: investment-api
  namespace: investment
spec:
  type: ClusterIP
  selector:
    app: investment-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
